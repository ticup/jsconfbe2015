var React = require('react');

/*
    components/YourAppComponent.js: Use this component as the root of the View of your application.
 */

/*
    TODO: Create other components, import and render them in here or in children of those components.
    TODO: render this view and insert in the actual DOM (in app.js).
 */

var YourAppComponent = React.createClass({
    render: function () {
        return (
            <div></div>
        );
    }
});

module.exports = YourAppComponent;