var Dispatcher     = require('../dispatcher');
var ActionCreators = require('../actions/ActionCreators');
var comm           = require('../comm');

/*
    utils/WebAPI: Exposes methods that directly handle communication with the web server.
 */

module.exports = {
    setupListeners: function () {
        /*
            TODO: listen for events from the server and create appropriate actions.
         */
    },

    /*
        TODO: Expose an API that allows to send requests to the server.
     */
};