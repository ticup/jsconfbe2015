/*
    utils/WebAPIUtils.js: Exposes helper functions that de-/serialize data from/to the server.
 */

module.exports = {
    /*
        Todo: Add helper methods here that de-/serialize data that comes and goes from and to the server.
     */
    serializeYourData: function (raw_data) {
        return "deserialized data";
    },

    deserializeYourData: function (data) {
        return "raw data";
    }
};